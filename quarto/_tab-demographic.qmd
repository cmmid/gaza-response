```{r, include=FALSE}
plot_data_timeseries <- map(summary_daily_org, 
                            ~ filter(., !grepl("other|prefer no", tolower(label))) |> 
                              mutate(label = fct_drop(label)))
```

##### {width="50%"}

```{r}
#| title: "Pre-war to now"
plot_bmi_units <- plot_time_series_statistics(
      plot_data_timeseries,
      #summary_variable = "bmi",
      strata = meta_params$stratum
    )
plot_bmi_percent <- plot_time_series_statistics(
      plot_data_timeseries,
      #summary_variable = "bmi_change_percent_prewar",
      strata = meta_params$stratum
    )

subplot(plot_bmi_units, plot_bmi_percent)
```

##### {width="50%"}

```{r}
#| title: "BMI category, % weekly participants"
#| fig-align: center
plot_bmicategory_proportions_time_series(
     plot_data_timeseries,
     strata = meta_params$stratum
    ) |> 
  ggplotly()
```
